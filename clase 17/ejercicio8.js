const nombre = 'ramiro';
const edad = '31';

console.log(`hola ${nombre}, asi que tienes ${edad} años`);
document.write(`hola ${nombre}, asi que tienes ${edad} años`);

const edad1 = 18;
const edad2 = 38;

const mensaje1 = `la cuota para persona de ${edad1} es de ${edad1 >= 21 ? 500: 350}`;
console.log(mensaje1);

const mensaje2 = `la cuota para persona de ${edad2} es de ${edad2 >= 21 ? 500: 350}`;
console.log(mensaje2);